#!/bin/bash
# Kanka Bulk Prepare Wrapper Script
# Automatically activates virtual environment and runs kanka-bulk-prepare.py

cd "$(dirname "$0")"
source ../../venv/bin/activate
python kanka-bulk-prepare.py "$@"
